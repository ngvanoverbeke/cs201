#include <vector>
#include <string>
#include <iostream>

 void getUserStrings(std::vector<std::string>& words) {
	 std::string inputWord;
	 while (inputWord != "end")
	 {
		 std::cin >> inputWord;
	 }
	 
}